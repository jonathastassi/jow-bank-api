
const handleResponseSuccess = require('./handle_response_success')
const handleResponseError = require('./handle_response_error')

module.exports = {
  handleResponseSuccess,
  handleResponseError
}
